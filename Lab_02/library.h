#ifndef LIBRARY_H
#define LIBRARY_H

namespace library{
    struct Library;
    struct Book;
    struct TechBook;
    struct ArtBook;
    struct ChildBook;
    union BookType;
    enum bookTypeKey;
    enum TechBookType;
    enum ArtBookType;
    enum ChildBookType;
}

#endif // LIBRARY_H
